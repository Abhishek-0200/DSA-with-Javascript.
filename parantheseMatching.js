function ParantheseMatching(input)
{
    let arr = []
    for(let i = 0;i<input.lenght;i++)
    {
        if(input[i] == "("  || input[i] == ")")
        arr.push(input[i]);
        if
    }
}

let input =  "(())";


console.log(ParantheseMatching(input))